enum Direction
{
    North,
    South,
    East,
    West,
}

enum Shape
{
    Circle(float),
    Rect(float, float),
}

float area(Shape s)
{
    switch (s)
    {
        case Shape::Circle(r):
        {
            return r * r;
        }
        case Shape::Rect(w, h):
        {
            return w * h;
        }
    }
}

void main()
{
    Direction d = Direction::North;
    Shape s = Shape::Circle(3.0);
    float a = area(s);
}
