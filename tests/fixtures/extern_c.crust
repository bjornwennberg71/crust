// extern "C" block — declare C functions to call from crust
extern "C"
{
    void printf(*char fmt);
    int strlen(*char s);
    *void malloc(int size);
}

void main()
{
    printf("hello from crust\n");
    int n = strlen("hello");
}
