// async/await: C#-style — `async` before the declaration, prefix `await` consumes.
// crust adds the tokio runtime automatically when async is used.

async string fetch()
{
    return "data";
}

async void run()
{
    string data = await fetch();
    println("{}", data);
}

async void main()
{
    await run();
}
