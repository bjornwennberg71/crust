// use statement

// type alias
type Meters = float;
type Seconds = float;

// constants
const int MAX_SPEED = 300;
static float PI = 3.14159;

// derive + pub struct with pub fields
#derive(Debug, Clone)
public struct Vehicle
{
    public float speed;
    public bool  active;
    public int   id;
}

// impl block — new style: this, mutable, static
impl Vehicle
{
    public static Vehicle new(float speed, int id)
    {
        return Vehicle { speed: speed, active: true, id: id };
    }

    public bool is_fast()
    {
        return this.speed > 100.0;
    }

    public void accelerate(float amount) mutable
    {
        this.speed += amount;
    }

    public void stop() mutable
    {
        this.active = false;
        this.speed = 0.0;
    }
}

// enum with tuple variants
#derive(Debug)
public enum Status
{
    Ok(int),
    Error(int, bool),
    Pending,
}

// generics in type position, Option, Result
Option<int> find_vehicle(Vec<Vehicle> fleet, int id)
{
    for (int i = 0; i < 10; i = i + 1)
    {
        if (fleet[i].id == id)
        {
            return Some(i);
        }
    }
    return None;
}

// closures, compound assignment, logical ops, unary
void demo_closures()
{
    auto double  = lambda(int x) { return x * 2; };
    auto is_even = lambda(int x) { return x % 2 == 0; };

    int val  = double(5);
    bool even = is_even(4);

    mutable int counter = 0;
    counter += 1;
    counter -= 1;
    counter *= 3;
    counter /= 3;

    bool flag    = true;
    bool flipped = !flag;
    int neg      = -42;

    bool both   = flag && !flipped;
    bool either = flag || flipped;
}

// conditional binding (canonical auto form + Rust if-let alias), else if chain
void demo_control(Option<int> maybe)
{
    if (auto Some(n) = maybe)
    {
        int doubled = n * 2;
    }

    if (maybe == None)
    {
        return;
    }
    else if (maybe == Some(0))
    {
        return;
    }
    else
    {
        return;
    }
}

// tuples
(int, float) demo_tuples()
{
    (int, float) pair = (1, 2.0);
    int a   = pair.0;
    float b = pair.1;
    return (a, b);
}

// arrays, index access, cast
void demo_arrays()
{
    int arr[4] = [10, 20, 30, 40];
    int first = arr[0];
    float f = (float)first;
}

// string literals
void demo_strings()
{
    auto greeting = "hello, world";
    auto name = "crust";
}

// match on int, default
int demo_match(int n)
{
    match n
    {
        0:        { return -1; }
        1 | 2:    { return 0; }
        default:  { return 1; }
    }
}

// unsafe block
void demo_unsafe()
{
    int x = 42;
    unsafe
    {
        int y = x * 2;
    }
}

// Result
Result<int, bool> parse_and_double(int n)
{
    int result = n * 2;
    return Ok(result);
}

// public function
public void main()
{
    mutable Vehicle v = Vehicle::new(50.0, 1);
    v.accelerate(60.0);

    bool fast = v.is_fast();

    Status s = Status::Ok(200);

    switch (s)
    {
        case Status::Ok(code):        { int c = code; }
        case Status::Error(code, b):  { int c = code; }
        case Status::Pending:         { return; }
    }

    demo_closures();
    auto t = demo_tuples();
    demo_arrays();
    demo_strings();
    int m = demo_match(2);
    demo_unsafe();
}
