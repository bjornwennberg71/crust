// trait declaration — abstract method + default method
trait Greet
{
    string hello();                  // must implement hello()

    string goodbye()                 // default impl goodbye()
    {
        return "goodbye";
    }
}

trait Area
{
    float area();
}

// structs
public struct Person
{
    public string name;
}

public struct Circle
{
    public float radius;
}

// impl Greet for Person
impl Greet for Person
{
    public string hello()
    {
        return format("Hello, {}!", this.name);
    }
}

// impl Area for Circle
impl Area for Circle
{
    public float area()
    {
        return 3.14159 * this.radius * this.radius;
    }
}

// trait as parameter type
void print_greeting(impl Greet item)
{
    string msg = item.hello();
    println("{}", msg);
}

void print_area(impl Area shape)
{
    println("area = {}", shape.area());
}

void main()
{
    Person p = Person { name: "Alice" };
    Circle c = Circle { radius: 5.0 };

    print_greeting(p);
    print_area(c);

    string bye = p.goodbye();
    println("{}", bye);
}
