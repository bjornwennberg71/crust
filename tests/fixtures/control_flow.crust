int abs(int x)
{
    if (x < 0)
    {
        return 0 - x;
    }
    else
    {
        return x;
    }
}

int count_down(int n)
{
    mutable int i = n;
    while (i > 0)
    {
        i = i - 1;
    }
    return i;
}

void main()
{
    int a = abs(0 - 5);
    int b = count_down(3);
}
