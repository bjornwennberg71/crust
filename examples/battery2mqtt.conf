# battery2mqtt configuration

mqtt_host        = localhost
mqtt_port        = 1883
topic_prefix     = battery

# number of independent battery packs
num_batteries    = 2

# modules per battery (max 10)
modules_per_battery = 4

# publish interval in milliseconds
interval_ms      = 1000
